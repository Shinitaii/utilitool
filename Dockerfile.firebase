FROM node:20-alpine

# Install Java 21+ and curl (required for Firebase emulator and health checks)
RUN apk add --no-cache openjdk21-jre curl

# Install Firebase tools globally
RUN npm install -g firebase-tools

WORKDIR /app

CMD ["firebase", "emulators:start", "--project=utilitool-test"]
